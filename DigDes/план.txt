2 действия:
 - открывает исходный файл и считает кол-во уникальных слов, вытаскивая построчно.
   Собираются в список?, сразу упорядоченный по уменьшение частоты.
 - создает новый файл и записывает туда весь список.

конечно, нужна защита от дурака.

чтение, запись асинхронны, так как текста могут быть очень большими.

отдельный тип UniqueWord с полями word и frequency (строгая типизация).
